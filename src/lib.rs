//!
//! This is a constant time unsigned integer library.
//! It is **work in progress** and offers different implementations,
//! starting out with some rather slow and generic ones.
//!
//! In this file you find the generic structs and traits used by all
//! implementations. But this is fixed to the implementation available right now.

pub mod util;
pub mod cuint32;

// #[derive(Debug,PartialEq,Eq,Clone)]
// pub struct CUint {}
